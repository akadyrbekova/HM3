import { useState } from "react";
import TodoList from "../component/TodoList";

const TodoListFeatures = () => {
  const [check, setCheck] = useState(false);

  return <>{/* <TodoList check={check} setCheck={setCheck} /> */}</>;
};

export default TodoListFeatures;
